console.log("Faculty Management System loaded");

function addFaculty(name, subject) {
  console.log(`Faculty Added: ${name} - ${subject}`);
}

function searchFaculty(name) {
  console.log(`Searching for Faculty: ${name}`);
}